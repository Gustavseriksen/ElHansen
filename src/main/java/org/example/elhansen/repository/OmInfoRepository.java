package org.example.elhansen.repository;

import org.example.elhansen.model.OmInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface OmInfoRepository extends JpaRepository<OmInfo, Integer> {
}
