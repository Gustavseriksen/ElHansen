package org.example.elhansen.repository;

import org.example.elhansen.model.KontaktInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KontaktInfoRepository extends JpaRepository<KontaktInfo, Integer> {
}
